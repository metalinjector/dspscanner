# DSP Scanner - Python зависимости
# Установка:  pip install -r requirements.txt

# --- Графический интерфейс (современный стандарт Qt6 / Fluent-стиль) -------
# PySide6 включает QtSvg (используется для иконки приложения), QtWidgets,
# QtCore, QtGui — всё в одном пакете.
PySide6>=6.6,<7.0

# --- Чтение DOCX -------------------------------------------------------------
python-docx>=1.1.0

# --- Чтение легаси DOC (OLE2 / RTF / HTML / XML) ----------------------------
olefile>=0.47
striprtf>=0.0.26
beautifulsoup4>=4.12.0
lxml>=5.0.0
defusedxml>=0.7.1
charset-normalizer>=3.3.0

# --- Чтение PDF (новый функционал) ------------------------------------------
PyMuPDF>=1.24.0

# --- Экспорт отчётов ----------------------------------------------------------
openpyxl>=3.1.0

# --- Автоматизация MS Word через COM (только Windows, опционально) --------
pywin32>=306; sys_platform == "win32"

# --- OCR для сканированных PDF без текстового слоя -------------------------
# Python-обёртки не требуются: DSP Scanner запускает Tesseract напрямую.
# Бинарь можно установить в системе, указать в диалоге «Параметры» либо
# поместить переносимый комплект вместе с tessdata в папку Tesseract-OCR.
