# DSP Scanner 2.9.9 — зависимости только для CLI/headless-сервера
# Установка: python -m pip install -r requirements_cli.txt

python-docx>=1.1.0
olefile>=0.47
striprtf>=0.0.26
beautifulsoup4>=4.12.0
lxml>=5.0.0
defusedxml>=0.7.1
charset-normalizer>=3.3.0
PyMuPDF>=1.24.0
openpyxl>=3.1.0
pywin32>=306; sys_platform == "win32"
# OCR запускается напрямую через внешний бинарь Tesseract-OCR.
